<%= form_for(@rssfeed) do |f| %>
  <% if @rssfeed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rssfeed.errors.count, "error") %> prohibited this rssfeed from being saved:</h2>

      <ul>
      <% @rssfeed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
